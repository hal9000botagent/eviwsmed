/* EviWsMed — Medical-Elegant Custom Theme for Starlight */

@import url('https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;1,400&family=EB+Garamond:ital,wght@0,400;0,500;0,600;1,400;1,500&family=Cormorant+SC:wght@300;400;500;600&display=swap');

/* ═══════════════════════════════════════════════════════════════
   LIGHT MODE — Warm, Medical-Elegant Palette
   ═══════════════════════════════════════════════════════════════ */
:root[data-theme='light'] {
  /* Primary Colors */
  --sl-color-accent-low: #f5ebe8;
  --sl-color-accent: #6B1D2A;
  --sl-color-accent-high: #4a1019;
  
  /* Text Colors */
  --sl-color-white: #2C2C2C;
  --sl-color-gray-1: #6B6155;
  --sl-color-gray-2: #2C2C2C;
  --sl-color-gray-3: #6B6155;
  --sl-color-gray-4: #D4CBBA;
  --sl-color-gray-5: #D4CBBA;
  --sl-color-gray-6: #F5F0E8;
  --sl-color-black: #FFFDF7;
  
  /* Backgrounds */
  --sl-color-bg: #F5F0E8;
  --sl-color-bg-nav: #EDE7DA;
  --sl-color-bg-sidebar: #EDE7DA;
  --sl-color-text-accent: #6B1D2A;
  
  /* Custom Accents */
  --eviwsmed-gold: #A8884A;
  --eviwsmed-green: #2D4A3E;
  --eviwsmed-burgundy: #6B1D2A;
  
  /* Shadows */
  --shadow-subtle: 0 2px 12px rgba(107, 29, 42, 0.08);
  --shadow-medium: 0 4px 20px rgba(107, 29, 42, 0.12);
}

/* ═══════════════════════════════════════════════════════════════
   DARK MODE — Warm Night Palette
   ═══════════════════════════════════════════════════════════════ */
:root[data-theme='dark'] {
  /* Primary Colors */
  --sl-color-accent-low: #3a1a22;
  --sl-color-accent: #C4586A;
  --sl-color-accent-high: #f3d0d6;
  
  /* Text Colors */
  --sl-color-white: #E2DCD2;
  --sl-color-gray-1: #E2DCD2;
  --sl-color-gray-2: #9C9487;
  --sl-color-gray-3: #9C9487;
  --sl-color-gray-4: #3A3A42;
  --sl-color-gray-5: #3A3A42;
  --sl-color-gray-6: #1A1A1F;
  --sl-color-black: #242429;
  
  /* Backgrounds */
  --sl-color-bg: #1A1A1F;
  --sl-color-bg-nav: #2A2A30;
  --sl-color-bg-sidebar: #2A2A30;
  --sl-color-text-accent: #C4586A;
  
  /* Custom Accents */
  --eviwsmed-gold: #C4A96A;
  --eviwsmed-green: #5A9E82;
  --eviwsmed-rose: #C4586A;
  
  /* Shadows */
  --shadow-subtle: 0 2px 12px rgba(0, 0, 0, 0.4);
  --shadow-medium: 0 4px 20px rgba(0, 0, 0, 0.5);
}

/* ═══════════════════════════════════════════════════════════════
   TYPOGRAPHY — Playfair Display + EB Garamond
   ═══════════════════════════════════════════════════════════════ */
:root {
  --sl-font: 'EB Garamond', Georgia, serif;
  --sl-font-system: 'EB Garamond', Georgia, serif;
  --sl-line-height: 1.75;
}

/* Body Text */
body,
p,
li,
td,
th,
blockquote,
.sl-markdown-content,
.content-panel {
  font-family: 'EB Garamond', Georgia, serif;
  font-size: 1.1rem;
  line-height: 1.8;
  letter-spacing: 0.01em;
}

/* Headings: Playfair Display */
h1,
h2,
h3,
.sl-markdown-content h1,
.sl-markdown-content h2,
.sl-markdown-content h3 {
  font-family: 'Playfair Display', Georgia, serif !important;
  letter-spacing: -0.02em;
  color: var(--sl-color-text-accent);
}

h1 {
  font-weight: 700;
  font-size: 2.5rem;
  line-height: 1.2;
  margin-bottom: 1.5rem;
}

h2 {
  font-weight: 700;
  font-size: 1.8rem;
  line-height: 1.3;
  margin-top: 2.5rem;
  margin-bottom: 1.2rem;
  border-bottom: 2px solid var(--eviwsmed-gold);
  padding-bottom: 0.5rem;
}

h3 {
  font-weight: 400;
  font-style: italic;
  font-size: 1.4rem;
  line-height: 1.4;
  margin-top: 2rem;
  margin-bottom: 1rem;
  color: var(--eviwsmed-green);
}

h4 {
  font-family: 'Cormorant SC', serif;
  font-weight: 500;
  letter-spacing: 0.08em;
  font-size: 1.1rem;
  text-transform: uppercase;
  color: var(--sl-color-gray-1);
  margin-top: 1.5rem;
  margin-bottom: 0.8rem;
}

/* Navigation & Sidebar: Cormorant SC */
nav,
starlight-menu-button,
.sidebar-content,
.sidebar-content a,
[data-pagefind-ignore] a,
.sl-sidebar a,
.sl-sidebar summary,
.sl-sidebar span,
.site-title {
  font-family: 'Cormorant SC', serif !important;
  font-weight: 500;
  letter-spacing: 0.08em;
}

.site-title {
  font-family: 'Playfair Display', Georgia, serif !important;
  font-weight: 700;
  font-size: 1.5rem;
  color: var(--sl-color-accent);
  letter-spacing: -0.01em;
}

/* ═══════════════════════════════════════════════════════════════
   SIDEBAR — Warm, Elegant
   ═══════════════════════════════════════════════════════════════ */
.sidebar-content {
  background: var(--sl-color-bg-sidebar);
  border-right: 1px solid var(--eviwsmed-gold);
}

.sidebar-content a {
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  border-radius: 6px;
  padding: 0.6rem 1rem;
}

.sidebar-content a:hover {
  background: var(--sl-color-accent-low);
  color: var(--sl-color-accent);
  transform: translateX(3px);
}

.sidebar-content a[aria-current='page'] {
  background: var(--sl-color-accent);
  color: var(--sl-color-black);
  font-weight: 600;
  box-shadow: var(--shadow-subtle);
}

/* Sidebar Group Titles */
.sidebar-content summary {
  color: var(--eviwsmed-gold);
  font-size: 0.85rem;
  text-transform: uppercase;
  letter-spacing: 0.12em;
  margin-top: 1.5rem;
  margin-bottom: 0.5rem;
  font-weight: 600;
}

/* ═══════════════════════════════════════════════════════════════
   HERO SECTION (Article Pages)
   ═══════════════════════════════════════════════════════════════ */
.hero {
  border-bottom: 3px solid var(--eviwsmed-gold);
  background: linear-gradient(135deg, var(--sl-color-bg) 0%, var(--sl-color-black) 100%);
  padding: 3rem 2rem;
  border-radius: 0 0 12px 12px;
  margin-bottom: 2rem;
}

.hero h1 {
  font-family: 'Playfair Display', Georgia, serif !important;
  font-weight: 700;
  color: var(--sl-color-accent);
  margin-bottom: 1rem;
}

.hero .tagline {
  font-family: 'EB Garamond', Georgia, serif !important;
  font-size: 1.2rem !important;
  color: var(--sl-color-gray-1);
  line-height: 1.7;
}

/* Hero Action Buttons */
.hero .action a[data-type='primary'] {
  background: var(--sl-color-accent) !important;
  border-color: var(--sl-color-accent) !important;
  font-family: 'Cormorant SC', serif !important;
  letter-spacing: 0.12em;
  padding: 0.9rem 2rem;
  border-radius: 8px;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: var(--shadow-subtle);
}

.hero .action a[data-type='primary']:hover {
  transform: translateY(-2px);
  box-shadow: var(--shadow-medium);
}

.hero .action a[data-type='minimal'] {
  font-family: 'Cormorant SC', serif !important;
  letter-spacing: 0.12em;
  color: var(--eviwsmed-gold);
  border-color: var(--eviwsmed-gold);
}

/* ═══════════════════════════════════════════════════════════════
   DETAILS BLOCKS (🔬 Fachinfo) — Medical-Elegant Styling
   ═══════════════════════════════════════════════════════════════ */
details {
  background: var(--sl-color-black);
  border-left: 4px solid var(--eviwsmed-burgundy);
  border-radius: 8px;
  padding: 1.5rem;
  margin: 2rem 0;
  box-shadow: var(--shadow-subtle);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

details:hover {
  box-shadow: var(--shadow-medium);
  transform: translateX(3px);
}

details[open] {
  border-left-color: var(--eviwsmed-gold);
  background: linear-gradient(90deg, var(--sl-color-accent-low) 0%, var(--sl-color-black) 2%);
}

details summary {
  font-family: 'Cormorant SC', serif;
  font-weight: 600;
  font-size: 1.05rem;
  letter-spacing: 0.08em;
  color: var(--eviwsmed-burgundy);
  cursor: pointer;
  user-select: none;
  transition: color 0.3s ease;
  list-style: none;
  position: relative;
  padding-left: 1.5rem;
}

details summary::-webkit-details-marker {
  display: none;
}

details summary::before {
  content: '🔬';
  position: absolute;
  left: 0;
  font-size: 1.2rem;
  transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

details[open] summary::before {
  transform: rotate(15deg);
}

details summary:hover {
  color: var(--eviwsmed-gold);
}

details[open] summary {
  color: var(--eviwsmed-gold);
  margin-bottom: 1.2rem;
  padding-bottom: 0.8rem;
  border-bottom: 1px solid var(--sl-color-gray-4);
}

details > *:not(summary) {
  padding-left: 0.5rem;
}

/* ═══════════════════════════════════════════════════════════════
   TABLES — Clean, Medical Aesthetic
   ═══════════════════════════════════════════════════════════════ */
table {
  width: 100%;
  border-collapse: collapse;
  margin: 2rem 0;
  border-radius: 8px;
  overflow: hidden;
  box-shadow: var(--shadow-subtle);
}

thead {
  background: linear-gradient(135deg, var(--eviwsmed-burgundy) 0%, var(--sl-color-accent-high) 100%);
}

th {
  font-family: 'Cormorant SC', serif;
  font-weight: 500;
  font-size: 0.9rem;
  letter-spacing: 0.15em;
  text-transform: uppercase;
  color: #FFFDF7;
  padding: 1rem 1.2rem;
  text-align: left;
  border-bottom: 2px solid var(--eviwsmed-gold);
}

:root[data-theme='dark'] th {
  color: #E2DCD2;
}

tbody tr {
  transition: background 0.2s cubic-bezier(0.4, 0, 0.2, 1);
}

tbody tr:nth-child(odd) {
  background: var(--sl-color-black);
}

tbody tr:nth-child(even) {
  background: var(--sl-color-bg);
}

tbody tr:hover {
  background: var(--sl-color-accent-low);
  transform: scale(1.01);
}

td {
  padding: 0.9rem 1.2rem;
  border-bottom: 1px solid var(--sl-color-gray-4);
  line-height: 1.6;
}

tbody tr:last-child td {
  border-bottom: none;
}

/* ═══════════════════════════════════════════════════════════════
   STARLIGHT ADMONITIONS (Aside Blocks)
   ═══════════════════════════════════════════════════════════════ */
.starlight-aside {
  border-radius: 8px;
  border-left-width: 4px;
  padding: 1.2rem 1.5rem;
  margin: 1.5rem 0;
  box-shadow: var(--shadow-subtle);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.starlight-aside:hover {
  transform: translateX(3px);
  box-shadow: var(--shadow-medium);
}

.starlight-aside--note {
  border-color: var(--eviwsmed-green);
  background: linear-gradient(90deg, rgba(45, 74, 62, 0.08) 0%, transparent 3%);
}

.starlight-aside--tip {
  border-color: var(--eviwsmed-gold);
  background: linear-gradient(90deg, rgba(168, 136, 74, 0.08) 0%, transparent 3%);
}

.starlight-aside--caution {
  border-color: #d97706;
  background: linear-gradient(90deg, rgba(217, 119, 6, 0.08) 0%, transparent 3%);
}

.starlight-aside--danger {
  border-color: var(--eviwsmed-burgundy);
  background: linear-gradient(90deg, rgba(107, 29, 42, 0.08) 0%, transparent 3%);
}

.starlight-aside__title {
  font-family: 'Cormorant SC', serif;
  font-weight: 600;
  letter-spacing: 0.08em;
  font-size: 1rem;
}

/* ═══════════════════════════════════════════════════════════════
   CARDS — Function Health Style
   ═══════════════════════════════════════════════════════════════ */
.card,
.sl-link-card {
  border: 1px solid var(--sl-color-gray-4);
  border-radius: 12px;
  padding: 1.8rem;
  background: var(--sl-color-black);
  box-shadow: var(--shadow-subtle);
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}

.sl-link-card:hover {
  transform: translateY(-4px) scale(1.01);
  border-color: var(--sl-color-accent);
  box-shadow: var(--shadow-medium);
  background: var(--sl-color-bg);
}

/* ═══════════════════════════════════════════════════════════════
   SEARCH
   ═══════════════════════════════════════════════════════════════ */
site-search button {
  font-family: 'Cormorant SC', serif !important;
  letter-spacing: 0.06em;
  border-radius: 8px;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

site-search button:hover {
  transform: scale(1.03);
  box-shadow: var(--shadow-subtle);
}

/* ═══════════════════════════════════════════════════════════════
   ORNAMENTAL DIVIDERS
   ═══════════════════════════════════════════════════════════════ */
hr {
  border: none;
  text-align: center;
  margin: 3rem 0;
  position: relative;
}

hr::before {
  content: '❧';
  color: var(--eviwsmed-gold);
  font-size: 1.4rem;
  opacity: 0.6;
  position: relative;
  display: inline-block;
}

hr::after {
  content: '';
  position: absolute;
  top: 50%;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, transparent, var(--eviwsmed-gold), transparent);
  z-index: -1;
}

/* ═══════════════════════════════════════════════════════════════
   LANDING PAGE SECTIONS (if used in Starlight docs)
   ═══════════════════════════════════════════════════════════════ */
.landing-section {
  margin: 3rem 0;
  padding: 2rem 0;
}

.landing-section h2 {
  text-align: center;
  margin-bottom: 2rem;
  font-size: 2rem;
  color: var(--sl-color-text-accent);
  border-bottom: none;
}

.landing-section.popular-articles {
  margin-top: 4rem;
  padding-top: 3rem;
  border-top: 2px solid var(--eviwsmed-gold);
}

.trust-banner {
  margin-top: 3rem;
  padding: 2rem;
  background: linear-gradient(135deg, var(--sl-color-accent-low) 0%, transparent 100%);
  border-left: 4px solid var(--eviwsmed-gold);
  border-radius: 8px;
}

/* ═══════════════════════════════════════════════════════════════
   CODE BLOCKS
   ═══════════════════════════════════════════════════════════════ */
pre,
code {
  font-family: 'SF Mono', 'Monaco', 'Inconsolata', 'Fira Code', monospace;
  border-radius: 6px;
}

pre {
  padding: 1.2rem;
  margin: 1.5rem 0;
  box-shadow: var(--shadow-subtle);
  border: 1px solid var(--sl-color-gray-4);
}

:not(pre) > code {
  background: var(--sl-color-accent-low);
  color: var(--eviwsmed-burgundy);
  padding: 0.2rem 0.5rem;
  border-radius: 4px;
  font-size: 0.9em;
  border: 1px solid var(--sl-color-gray-4);
}

/* ═══════════════════════════════════════════════════════════════
   BLOCKQUOTES
   ═══════════════════════════════════════════════════════════════ */
blockquote {
  border-left: 4px solid var(--eviwsmed-gold);
  padding-left: 1.5rem;
  margin: 2rem 0;
  font-style: italic;
  color: var(--sl-color-gray-1);
  background: linear-gradient(90deg, var(--sl-color-accent-low) 0%, transparent 5%);
  padding: 1.2rem 1.5rem;
  border-radius: 0 8px 8px 0;
}

blockquote cite {
  display: block;
  margin-top: 0.8rem;
  font-style: normal;
  font-size: 0.9rem;
  font-family: 'Cormorant SC', serif;
  letter-spacing: 0.05em;
  color: var(--eviwsmed-gold);
}

/* ═══════════════════════════════════════════════════════════════
   MOBILE RESPONSIVENESS
   ═══════════════════════════════════════════════════════════════ */
@media (max-width: 768px) {
  h1 {
    font-size: 2rem;
  }
  
  h2 {
    font-size: 1.5rem;
  }
  
  h3 {
    font-size: 1.2rem;
  }
  
  body,
  p,
  li {
    font-size: 1rem;
  }
  
  details {
    padding: 1rem;
  }
  
  table {
    font-size: 0.9rem;
  }
  
  th,
  td {
    padding: 0.7rem 0.8rem;
  }
}

/* ═══════════════════════════════════════════════════════════════
   SMOOTH TRANSITIONS
   ═══════════════════════════════════════════════════════════════ */
* {
  transition-property: background, border-color, color, box-shadow, transform;
  transition-duration: 0.3s;
  transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
}

a,
button,
summary {
  transition-property: all;
}
